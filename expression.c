#include <assert.h>
#include <ctype.h>
#include <stdlib.h>

#include "expression.h"

#undef NDEBUG // FORCE ASSERT ACTIVATION

bool term_is_valid_expression(term t) { return false; }

int expression_valuate(term t) { return 0; }
